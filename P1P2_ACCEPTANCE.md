# P1 / P2 本地验收记录

日期：2026-09-13。范围：接续 GLM 的本地修改，不覆盖线上部署、不使用真实付费 API。

## 验收结果

| 编号 | 本地实现与验证 | 限制 / 上线前核对 |
|---|---|---|
| P1-9 错误处理 | 原有固定错误分类；修复换一套的中途状态异常、坏图恢复后残留错误 | 服务商自定义错误码需按实际响应补充映射 |
| P1-10 API 预设 | 自定义预设/协议保存后不会重置；所有预设可选生图模型；保留文本/图片独立 Key、地址 | 实际渠道模型、余额与协议权限需真实联调 |
| P1-11 图文数据同源 | 外套纳入完整清单；移除可能矛盾的自由文本生图指令；拒绝空/重复 LOOK 编号 | 不承诺模型生成像素与清单完全一致，仍需人工检查 |
| P1-12 识别确认 | 可编辑属性和保留细节；改颜色/材质等时不再静默沿用冲突的旧细节；进入 API 设置再返回不丢失编辑 | 新识别会初始化新照片对应的确认字段，不沿用上一件衣服的修改 |
| P1-13 按需调用 | 默认文字；逐图生成、续查及下载；换一套仅一次文本请求；关闭生图和 Demo 不暴露收费操作 | 次数为业务调用说明，不是实际金额；异步 GET 轮询不等于新建生图任务 |
| P1-14 诊断 | INFO 日志启用；只允许固定字段，移除换行，错误不输出服务商原始响应 | 请求编号是本站诊断编号，不是所有平台的计费编号 |
| P1-15 运行环境 | 核心依赖固定；补充直接使用的 NumPy 版本；Python 3.10 和 3.12 回归通过 | Cloud 已部署解释器尚未核实；.python-version 不能自动修改它 |
| P1-16 测试/发布 | 142 项 pytest 通过，含跨页流程、安全、独立接口、异步续查、跨批次收藏 | CI 文件已有；未推送、未核实远程 Actions、未配置分支保护 |
| P2-17 搭配差异 | 比较上装/外套/下装/鞋/包，排除空值、“无”和匹配的原单品；验证原单品为下装的场景 | 基于字符串的启发式规则，不是视觉或语义相似度模型；拒绝后不自动付费重试 |
| P2-18 图片处理 | 先验证原图再预处理；纠正 EXIF、像素限制；真实内容哈希区分照片；先缩放后分配合成缓冲区；空上传和交叉裁剪边界阻止提交 | rembg 是可选能力，未安装，不作为云端已验收功能 |
| P2-19 移动端 | 隔离 Chrome 验证 320/390/768/1024/1440 宽度 Demo、收藏、PNG 下载；390 宽度完成上传到单图生成 | 用模拟服务商验证状态与界面，不代表真实生成质量或所有手机浏览器 |
| P2-20 历史/收藏/导出 | 每批独立编号；生图前存历史；后补图按批次同步；最多 20 套收藏可移除；图片/TXT 下载 | 最近 4 批历史；仅会话内存，刷新/断线可能丢失，不是云端个人衣橱 |
| P2-21 天气活动 | 手选气温、天气、通勤，真实页面到模拟规划接口验证参数传入 | 不查询城市天气，不额外引入天气 API |
| P2-22 偏好记忆 | 独立持久会话字段；跨页测试和浏览器流程验证禁用单品不丢失 | “喜欢”只作标记，不虚称自动训练或个性化学习 |
| P2-23 隐私与清除 | 准确说明服务器会话内存、第三方传输和计费；确认后清除内容，保留 API 配置和限流身份 | 清除会话不能取消平台任务或删除服务商已有数据；托管平台控制实际内存释放 |

## 测试命令

```bash
.venv/bin/python -m pytest -q
.venv/bin/python -m pip check
outputs/verify312/bin/python -m pytest -q
outputs/verify312/bin/python -m pip check
outputs/verify312/bin/python -m compileall -q app.py cloud_app.py stylemate
```

两套环境均为 142 passed，pip check 无依赖冲突。API 边界使用替身或 MockTransport，未消耗生图或文本额度。

API 设置的“返回应用”会恢复原确认页或结果页；上传页“继续上次搭配”可恢复既有结果、收藏入口和异步任务，不重新生成。已有任务仍使用所属批次的接口配置，不把新 Key 发送到旧任务的查询地址。

浏览器复现脚本位于忽略提交的 `outputs/p1p2-browser-smoke.cjs` 与 `outputs/p1p2-browser-upload.cjs`；通过 `STYLEMATE_PLAYWRIGHT` 指向本机已有的 Playwright 模块。测试窗口使用独立浏览器上下文，不读取已登录账号。

- 正常本地预览：`cloud_app.py`，端口 8502。
- 模拟接口页面：`outputs/p1p2-ui-fixture.py`，端口 8503，仅用于测试，验收后停止；不得发布到云端。
- 移动端截图：`outputs/p1p2-api-mobile.png`、`outputs/p1p2-results-mobile.png`、`outputs/p1p2-confirmed-result-mobile.png`。

## 关键实现约束

历史与收藏不能只按模型给出的 `look-01` 等编号关联，也不能假定 `history[0]` 永远是当前任务。批次编号由本地生成，收藏使用“批次 + LOOK”组合键；历史在任何生图请求前建立。这样失败、续查和多次生成不会把另一批的照片配进旧收藏。

工作目录没有 Git 元数据，未建立或修改 Git 远端。早期续改检查点保存在 `outputs/before-codex-p1p2-20260913.tar.gz`，不包含环境密钥文件；本次修改没有动训练脚本或数据目录。

## 尚未执行的上线步骤

1. 将应用、完整 `stylemate/` 包、测试、依赖和 CI 文件作为同一版本同步到 GitHub；不要只替换 app.py，也不要上传 outputs、.venv、.env 或个人密钥。
2. 检查 GitHub Actions 成功；如需要严格发布门禁，在 GitHub 设置必需测试状态并通过 PR 合并，而不是直接推 main。
3. 在 Community Cloud 核实 Python 3.12、启动入口 cloud_app.py 和部署日志。
4. 使用本人中转站 Key 做一次小额文本调用和一张图片生成，核实权限、队列/续查、下载和实际收费；未执行前不宣称线上联调通过。
