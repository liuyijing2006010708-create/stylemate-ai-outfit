# 安全政策

StyleMate 处理用户提供的 API Key、服装图片和第三方模型请求。请先阅读以下边界，再部署或报告问题。

## 报告安全问题

请优先在仓库 **Security** 页面使用 GitHub Private Vulnerability Reporting 的 **Report a vulnerability**，避免在公开 Issue 中披露可利用细节。

如果该入口不可用，请只创建不含漏洞细节和敏感数据的公开 Issue，请维护者提供私密沟通方式。仓库目前不公布安全邮箱，因此不要把报告发送到猜测的地址。

报告可包含受影响版本、影响范围、最小复现步骤和修复建议，但不得包含：

- API Key、Token、Cookie、账号或其他 credentials。
- 含个人信息的图片、日志或服务商响应。
- 针对不属于报告者的线上系统进行破坏性验证所得的数据。

维护者会在 GitHub 可用的沟通渠道中确认报告并评估修复；本项目目前不承诺固定响应时限。

## API Key 与会话数据

- 本地入口可以从 `.env` 读取 Key；`.env` 已被 Git 忽略，`.env.example` 只能保存空值或公开示例。
- 公开入口不会把服务器环境中的 `OPENAI_API_KEY` 交给访客。
- 页面配置的 Key、上传图片、搭配和生成结果暂存在当前 Streamlit 会话内存，不写入项目文件或数据库。
- 刷新、会话过期或服务重启可能清空数据；实际内存释放由托管平台管理。
- 清除本地会话不会取消已提交的第三方任务，也不会删除第三方已经保存的数据。

## 第三方 Provider

真实调用会把 API Key 作为鉴权信息发送到用户配置的 Provider，并把完成任务所需的图片、提示词和结构化请求发送给该服务。Provider 的日志、保留期限、训练用途、地区和费用政策不受本仓库控制。

用户应只配置可信的 HTTPS endpoint，并自行阅读相应隐私政策。不要在公共或多人共用的浏览器会话中输入长期有效的高权限 Key。

## 部署边界

- 项目会校验外部 URL、DNS 公网地址、重定向和返回图片，但不能替代 Provider 自身的安全控制。
- 当前限流状态位于单个 Python 进程；横向扩展时需要共享限流存储。
- 项目没有用户账号、持久化个人衣橱或跨设备同步。
- 自动化测试不会发送真实付费 API 请求，也不能验证第三方账号权限或数据处理实践。

部署建议见 [`DEPLOY.md`](DEPLOY.md)，架构安全边界见 [`docs/architecture.md`](docs/architecture.md)。
